---
description: AgentLab 后端工程规范（分层、命名、SSE、Patch、Tool）
globs:
  - apps/api/**
  - packages/**
alwaysApply: true
---

# AgentLab 后端工程规范

> **适用范围**：`apps/api/**` 和 `packages/{shared,agent-core}/**`。
> **执行强度**：MANDATORY。

## 1. 分层架构

```text
apps/api/src/
  index.ts                # 启动入口，只做 initDatabase + app.listen
  app.ts                  # Koa 实例 + 中间件 + 路由挂载
  config/                 # 环境变量、常量
  db/                     # SQLite 连接、schema 初始化
  llm/                    # LLM Provider 客户端封装
  tools/                  # Agent 可调用工具
  routes/                 # Koa Router，仅做路径 ↔ controller 绑定
  controllers/            # Koa Context 层，负责入参校验 + 返回值
  services/               # 业务编排，跨多个 repo / 多个外部调用
  repositories/           # SQLite 读写，只暴露领域对象
  eval/                   # 评测用例 / 评测器
```

> **RED LINE - 层依赖方向**（强制）
>
> - **MANDATORY**: 调用方向只能是 `routes → controllers → services → repositories / llm / tools`。
> - **FORBIDDEN**: repositories 引用 services；controllers 直接 import repositories（必须经 service）。
> - **FORBIDDEN**: services 直接读 `ctx.request`，HTTP 概念只在 controller 层出现。

## 2. 命名

- 文件：`xxx.controller.ts` / `xxx.service.ts` / `xxx.repository.ts`，单数。
- 导出类：`XxxController` / `XxxService` / `XxxRepository`，并在文件底部 `export const xxxX = new XxxX()` 暴露默认单例。
- DB 列：snake_case；TS 字段：camelCase。`xxxRepository` 内部 `serialize` 函数完成转换。

## 3. SSE / EventBus 规范

- 任何 Agent 运行时事件**必须**先写库（`agentEventRepository.create`），再由 `eventBusService` 自动 publish 到对应 `run:${runId}` 通道。
- 新增事件类型必须同时在 `packages/shared` 的 `AgentEventType` 加成员。
- SSE controller 必须：
  - 设置 `Content-Type: text/event-stream` + `Cache-Control: no-cache` + `X-Accel-Buffering: no`
  - 监听 `ctx.req.on('close')` 做清理（unsubscribe + clearInterval + stream.end）
  - 15s 一次心跳 (`:hb\n\n`)

## 4. Tool 系统

- 每个 tool 必须注册到 `ToolRegistry`，包含：
  - `schema` (`{ name, description, parameters }` 严格 OpenAI function spec)
  - `handler(payload, args)` 异步函数
- handler 内任何文件路径**必须**经 `resolveInsideProject()` 校验，禁止逃出项目沙箱。
- 任何执行命令的 tool 必须：白名单 + `timeout` ≤ 20s + 输出截断到 4 KB。

## 5. Patch / Diff 规范

- 所有 patch 应用必须先 `parseUnifiedDiff` 解析为 hunk。
- 已存在文件修改前必须写 snapshot 到 `.agent-lab/snapshots/<runId>/<file>`。
- rollback 必须能基于 metadata 完全还原 `createdFiles` / `modifiedFiles` / `deletedFiles` 三类。
- 验证步骤（lint/build）只能在白名单命令 + 项目根目录执行。

## 6. 数据库

- 所有表创建在 `db/database.ts:initDatabase()`，使用 `CREATE TABLE IF NOT EXISTS`。
- 新增列时优先用迁移脚本，禁止偷偷 `ALTER` 旧表（暂未引入迁移系统，新增列时在 PR 描述里明确说明影响）。
- 任何 repository 方法**必须**返回领域对象，不要把 raw row 暴露给上层。

## 7. 共享类型

- 所有跨前后端类型必须在 `packages/shared/src/index.ts` 声明并 `export`。
- 后端 / 前端任何地方禁止重复声明 `AgentEvent` / `AgentRunResult` 等核心类型。
- API 请求体校验必须用 zod schema（同文件 export 一个 `xxxRequestSchema`），controller 调 `safeParse`。

## 8. LLM Provider

- 仅 `llm/*.client.ts` 可以与外部 API 通讯。
- 每次调用必须返回 `{ message, usage, latencyMs }`，让上层能写 cost。
- 成本估算统一用 `estimateUsdCost(model, usage)`，价目表集中维护，禁止散落硬编码。

## 9. 提交前 checklist

- [ ] `pnpm typecheck`（4 个包）全过
- [ ] `pnpm lint` 0 error 0 warning
- [ ] 新增能力是否同步更新 `services/capability.service.ts` 状态
- [ ] 涉及共享类型是否在 `packages/shared` 同步加上
- [ ] 涉及 prompt 是否仍是单文件可读，未硬编码业务字段
